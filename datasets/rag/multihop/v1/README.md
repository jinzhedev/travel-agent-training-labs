# 多跳证据计划 v1

`plans.jsonl` 把复杂问题拆为具名 evidence slot。每个 slot 都有自己的查询、依赖和预期证据类型。

两种结构需要分清：

- 并行聚合：多个 slot 可以独立检索，最后比较或计算。
- 依赖型多跳：后一个 slot 的查询必须使用前一个 slot 返回的实体。只运行两次互不相关的检索，不算依赖型多跳。

CP03 使用显式 `evidence_plan_id` 训练证据规划与可观测性；CP04 才加入自动路由和自适应规划。这个边界能让学员先看清机制，再讨论由模型决定何时使用它。
