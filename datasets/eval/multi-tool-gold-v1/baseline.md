# 本地基线（2026-09-07）

本次运行覆盖 12 条任务、21 个冻结轮次。候选检索和真实模型计划均已运行完成，当前基线未达到全部通过标准。

控制变量：`tool-catalog-v1.5`、`training` 环境、`travel.read / booking.read` 权限、`allow_side_effects=false`、参考日期 `2026-09-07`。数据 SHA-256 为 `b012402d846bef998dcc8b69e55634490b8c422bd3807dbb4f51046025f0fac3`。

## 候选检索

| 策略 | 上限 | 必要工具召回 | 禁止工具暴露轮数 | 副作用暴露轮数 | 平均候选膨胀 | 通过轮数 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| full_catalog | 64 | 17/17 | 8 | 0 | 22.33 | 13/21 |
| namespace | 64 | 16/17 | 6 | 0 | 5.29 | 14/21 |
| retrieval | 5 | 15/17 | 6 | 0 | 2.67 | 13/21 |

三组越权暴露均为 0。禁止工具暴露来自当前轮不应使用的后续只读工具，不是写工具泄漏。全量候选虽然没有漏召回，仍然会提前暴露后续工具；当前检索函数没有按 Observation 管理工具依赖。

## 真实模型计划

使用 `glm-5.3-flash`、retrieval Top-5、服务商默认采样设置运行全部 21 轮。调用 OpenAI 兼容接口获取 JSON 工具计划，没有执行工具，也没有使用 Dify Workflow。

- 动作判断：20/21；`MT-010` 应澄清，模型选择了拒绝。
- 候选必要工具召回：15/17。
- 当前轮全部检查通过：11/21。
- 依赖检查未发现提前调用或伪造前序 ID；这不代表必要调用都已完成。
- 副作用暴露、越权暴露均为 0。
- 最终任务结果：`not_evaluated`。

两处漏召回都是 `itinerary.build_draft`：`MT-005/found` 与 `MT-007/ready` 已有生成草案所需结果，但 Top-5 中没有草案工具。模型转而查询无障碍或景点详情，首次偏差归候选层。`MT-007/waiting_weather` 的天气工具已在候选中，模型又增加了无障碍详情查询，归选择层。

这是单次模型基线，不能据此声称模型稳定达到某个准确率。后续优化应固定本组数据和模型，修改一项变量后重复运行。该实验的作用是保留失败样本及其首次偏差，不把扩大候选集合直接当作改进。

## 本机原始记录

以下运行目录位于 Git 忽略的 `var/reports/lab02-multi-tool-gold/`，包含逐轮结果和原始预测；换机器后按实验命令重新生成：

- full_catalog：`20260907T152101Z-c85dc362`
- namespace：`20260907T152101Z-94c31b5c`
- retrieval：`20260907T152101Z-a2844b9a`
- 真实模型：`20260907T152102Z-10ef7be1`
- 导入真实预测重新评分：`20260907T152255Z-dd656d3a`，指标与原运行一致。

评分模块和脚本的相关测试共 48 项通过，包含数据交叉校验、并行顺序、缺行／重复预测、暴露、依赖、错误参数及停止控制。原有 40 条 Tool Calling 数据也通过恢复后的目录契约校验。
